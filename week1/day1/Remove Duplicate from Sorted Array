class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
      int n = nums.size();
      int j = 1;
      int i = 0;
      while(i<n-1) {
        if(nums[i]!=nums[i+1]) {
            nums[j] = nums[i+1];
            j++;
        }
        i++;
      }
      return j;
     }
};
